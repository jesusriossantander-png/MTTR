# La planilla completa nunca va al repo (contiene datos individuales)
*.xlsx
records.json

# Credenciales locales (nunca subirlas)
service-account.json
clave-actualizar.txt

# Temporales
data_test.js
__pycache__/
*.pyc
